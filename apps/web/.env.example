# Copie para ".env" ao rodar o frontend fora do Docker: `cp .env.example .env`
VITE_API_URL=http://localhost:3333

# Deve ter o MESMO valor de API_TOKEN em apps/api/.env — é embutido no bundle
# em build time (Vite), então NÃO é um segredo real, apenas identifica este
# frontend para a API (ver apps/api/src/presentation/http/middlewares/apiTokenAuth.ts).
VITE_API_TOKEN=a3c195592e348f4e83eaa82956c6d077cc747c3852b6daa44fd64214ec3c294b
